[phases.setup]
nixPkgs = ["nodejs-18_x", "python3", "pip"]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install yt-dlp",
  "npm install"
]

[start]
cmd = "npm start"

